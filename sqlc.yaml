version: "1"
packages:
  - schema: "./db/migration/"
    name: "db"
    queries: "./db/query/"
    engine: "postgresql"
    path: "./db/sqlc/"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: false
    emit_exact_table_names: false
    emit_empty_slices: true